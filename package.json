{
  "name": "fmgordillo-react-testing",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "express": "^4.16.1"
  },
  "scripts": {
    "start": "node index.js",
    "build": "cd ui-react && yarn && yarn build"
  },
  "engines": {
    "node": "^6.0.0",
    "npm": "^5.0.0"
  }
}
